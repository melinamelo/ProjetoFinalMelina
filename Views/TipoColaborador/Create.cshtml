@model ProjetoFinalMelina.Models.TipoColaborador

@{
    ViewData["Title"] = "Create";
}

<h1 class="pags">Adicionar um novo tipo de colaborador</h1>

<div class="row">
    <div class="col-md-4">
        <form asp-action="Create" class="form-edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="TipoColaboradorNome" class="control-label"></label>
                <input asp-for="TipoColaboradorNome" class="form-control" />
                <span asp-validation-for="TipoColaboradorNome" class="text-danger"></span>
            </div>
            <div class="button-criar">
                <input type="submit" value="Adicionar" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index" class="a-edit3">Voltar para a lista de tipos de colaboradores</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
