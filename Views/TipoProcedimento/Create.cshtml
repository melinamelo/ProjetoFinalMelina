@model ProjetoFinalMelina.Models.TipoProcedimento

@{
    ViewData["Title"] = "Create";
}

<h1 class="pags">Adicionar novo tipo de procedimento</h1>

<div class="row">
    <div class="col-md-4">
        <form asp-action="Create" class="form-edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="TipoProcedimentoNome" class="control-label"></label>
                <input asp-for="TipoProcedimentoNome" class="form-control" />
                <span asp-validation-for="TipoProcedimentoNome" class="text-danger"></span>
            </div>
            <div class="button-criar">
                <input type="submit" value="Adicionar" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index" class="a-edit3">Voltar para a lista de tipos de procedimentos</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
